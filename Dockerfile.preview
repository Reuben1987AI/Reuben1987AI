FROM python:3.12-slim

RUN pip install --no-cache-dir grip

WORKDIR /doc

EXPOSE 5000

# grip serves README.md on all interfaces so the host port-mapping reaches it.
# grip has built-in live reload: it polls the file mtime and refreshes browsers.
CMD ["grip", "README.md", "0.0.0.0:5000"]
